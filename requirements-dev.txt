# ClaimGuard development + CI dependencies.
-r requirements.txt

pytest>=8               # unit + integration tests
pytest-cov>=5           # coverage reporting
httpx>=0.27             # FastAPI TestClient transport for API tests
ruff>=0.4               # lint + format (fast, single tool)
moto>=5.0               # in-process AWS mock (S3 + DynamoDB) to test the Lambda path
